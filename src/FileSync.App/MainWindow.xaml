<Window x:Class="FileSync.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="File Sync Prototype"
        Width="960"
        Height="620">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <TextBlock Text="Sync Root:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox Width="500" Text="{Binding SyncRootPath, UpdateSourceTrigger=PropertyChanged}" />
            <Button Margin="8,0,0,0" Padding="12,6" Content="Initialize Sync Root" Click="InitializeSyncRoot_Click" />
            <Button Margin="8,0,0,0" Padding="12,6" Content="Unregister Sync Root" Click="UnregisterSyncRoot_Click" />
            <Button Margin="8,0,0,0" Padding="12,6" Content="Hydrate Selected" Click="HydrateSelected_Click" />
            <Button Margin="8,0,0,0" Padding="12,6" Content="Delete Selected Entry" Click="DeleteSelectedEntry_Click" />
            <Button Margin="8,0,0,0" Padding="12,6" Content="Clear Failed Entries" Click="ClearFailedEntries_Click" />
        </StackPanel>

        <Border Grid.Row="1"
                Margin="0,12,0,12"
                Padding="20"
                Background="#F3F7FC"
                BorderBrush="#2F6DB5"
                BorderThickness="1"
                AllowDrop="True"
                Drop="DropZone_Drop"
                DragOver="DropZone_DragOver">
            <TextBlock Text="Drag and drop files here to upload and sync" FontSize="16" HorizontalAlignment="Center" />
        </Border>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Files}"
                  SelectedItem="{Binding SelectedFile, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="File Name" Binding="{Binding FileName}" Width="3*" />
                <DataGridTextColumn Header="Size (Bytes)" Binding="{Binding SizeBytes}" Width="2*" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="2*" />
                <DataGridTextColumn Header="Updated (UTC)" Binding="{Binding UpdatedAtUtc}" Width="3*" />
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="3" Margin="0,10,0,0" Text="{Binding LastMessage}" TextWrapping="Wrap" />
    </Grid>
</Window>
